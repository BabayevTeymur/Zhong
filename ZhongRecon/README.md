# Zhong – LAN Recon Toolkit

Zhong is a LAN-focused reconnaissance and mapping tool. It wraps `nmap` into a parallel,
GUI-driven workflow aimed at defenders, students, and lab environments. The GUI includes
full-range port sweeps, hop-by-hop traceroutes rendered onto a topology canvas, themed UI
styling, and timestamped report exports (JSON/Markdown/HTML).
